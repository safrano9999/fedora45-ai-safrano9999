# Source of truth: SCRIPTS/githubactions. Generated copies are overwritten.
name: Fedora45 container preparation
run-name: 'Fedora45 preparation [prepare:${{ inputs.run_id }}]'

on:
  workflow_dispatch:
    inputs:
      source_snapshot:
        description: Exact latest-source snapshot resolved once by n8n after the release or explicit Safrano upgrade gate
        required: true
        type: string
      run_id:
        description: n8n execution correlation ID
        required: true
        type: string
      upgrade_safrano9999:
        description: "Explicit source upgrade against published latest images; start at the earliest changed stage"
        required: false
        type: boolean
        default: false
      upgrade_build_deps:
        description: "Refresh allowlisted build pins and JSON-configured base-image release lines"
        required: false
        type: boolean
        default: false
      force_prepare:
        description: "Explicit operator rebuild with refreshed dependencies; automatic n8n gate remains version-only"
        required: false
        type: boolean
        default: false
      validate_only:
        description: Test both current target runtimes without publishing pin changes
        required: true
        type: boolean
        default: false

concurrency:
  group: fedora45-container-preparation
  cancel-in-progress: false

jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ fromJSON(inputs.source_snapshot).source_commit }}
          token: ${{ secrets.GH_RELEASE_TOKEN || github.token }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - uses: actions/setup-node@v4
        with:
          node-version: '26'
      - name: Configure private repository access
        env:
          GH_TOKEN: ${{ secrets.GH_RELEASE_TOKEN || github.token }}
        run: gh auth setup-git
      - name: Prepare and test exact build inputs
        env:
          GH_TOKEN: ${{ secrets.GH_RELEASE_TOKEN || github.token }}
          FORCE_PREPARE: ${{ inputs.force_prepare }}
          UPGRADE_BUILD_DEPS: ${{ inputs.upgrade_build_deps }}
          UPGRADE_SAFRANO9999: ${{ inputs.upgrade_safrano9999 }}
          VALIDATE_ONLY: ${{ inputs.validate_only }}
          SOURCE_SNAPSHOT_JSON: ${{ inputs.source_snapshot }}
        shell: bash
        run: |
          set -euo pipefail
          flags=()
          if [ "$FORCE_PREPARE" = true ]; then flags+=(--force-prepare); fi
          if [ "$UPGRADE_SAFRANO9999" = true ]; then flags+=(--upgrade-safrano9999); fi
          if [ "$UPGRADE_BUILD_DEPS" = true ]; then flags+=(--upgrade-build-deps); fi
          if [ "$VALIDATE_ONLY" = true ]; then flags+=(--validate-only); fi
          python3 -c 'import os; from pathlib import Path; Path(os.environ["RUNNER_TEMP"], "source-snapshot.json").write_text(os.environ["SOURCE_SNAPSHOT_JSON"])'
          python3 upgrade-loop/prepare-container.py "${flags[@]}" \
            --source-snapshot "$RUNNER_TEMP/source-snapshot.json" \
            --report "$RUNNER_TEMP/container-preparation.json"
      - name: Preserve preparation evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: container-preparation
          path: ${{ runner.temp }}/container-preparation.json
          if-no-files-found: error
